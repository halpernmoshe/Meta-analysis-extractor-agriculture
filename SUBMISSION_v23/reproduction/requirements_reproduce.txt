# Requirements for reproducing validation stats, figures, and DOCX
# No API keys or PDFs needed -- operates on pre-extracted data only.
#
# Install:  pip install -r requirements_reproduce.txt

numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12
python-docx>=1.0
openpyxl>=3.1
