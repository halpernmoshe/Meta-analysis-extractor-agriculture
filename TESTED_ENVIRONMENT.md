# Tested environment

The complete `run.ps1` workflow was executed successfully in the
following environment during the final round-2 audit:

- Microsoft Windows NT 10.0.26200.0
- Python 3.12.10, 64-bit
- matplotlib 3.10.8
- NumPy 2.4.0
- pandas 2.3.3

`requirements.txt` retains minimum compatible versions for installation.
This file records the exact tested versions rather than imposing them as
the only supported environment.
